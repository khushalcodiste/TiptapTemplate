export const { UNSPLASH_API_KEY } = process.env;

export const { FREEPIK_API_KEY } = process.env;

export const { GOOGLE_IMAGE_API_KEY } = process.env;

export const { GOOGLE_IMAGE_API_CX } = process.env;

export const { IMAGE_SEARCH_PROVIDER } = process.env;

export const FILE_SIZE_LIMIT = 30 * 1024 * 1024; // 30 mb

export const NEXT_PUBLIC_ASSET_URL = 'https://s3.us-east-1.amazonaws.com/ai-avengers-content';

export const NODE_ENV = process.env.NODE_ENV || 'production';

export const FEATURE_DOC_VIEW_VISIBLE = process.env.NEXT_PUBLIC_FEATURE_DOC_VIEW_VISIBLE === 'true';
