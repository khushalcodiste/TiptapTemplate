import { Blockquote as TiptapBaseBlockquote } from '@tiptap/extension-blockquote';
import { Bold as TiptapBaseBold } from '@tiptap/extension-bold';
import { BulletList as TiptapBaseBulletList } from '@tiptap/extension-bullet-list';
import { Code as TiptapBaseCode } from '@tiptap/extension-code';
import { Dropcursor as TiptapBaseDropcursor } from '@tiptap/extension-dropcursor';
import { Gapcursor as TiptapBaseGapcursor } from '@tiptap/extension-gapcursor';
import { HardBreak as TiptapBaseHardBreak } from '@tiptap/extension-hard-break';
import { Heading as TiptapBaseHeading } from '@tiptap/extension-heading';
import { History as TiptapBaseHistory } from '@tiptap/extension-history';
import { HorizontalRule as TiptapBaseHorizontalRule } from '@tiptap/extension-horizontal-rule';
import { Image as TiptapImageExtension } from '@tiptap/extension-image';
import { Italic as TiptapBaseItalic } from '@tiptap/extension-italic';
import { ListItem as TiptapBaseListItem } from '@tiptap/extension-list-item';
import { OrderedList as TiptapBaseOrderedList } from '@tiptap/extension-ordered-list';
import { Paragraph as TiptapBaseParagraph } from '@tiptap/extension-paragraph';
import { Placeholder as TiptapBasePlaceholder } from '@tiptap/extension-placeholder';
import { Strike as TiptapBaseStrike } from '@tiptap/extension-strike';
import { Table as TiptapBaseTable } from '@tiptap/extension-table';
import { TableRow as TiptapBaseRow } from '@tiptap/extension-table-row';
import { Text as TiptapBaseText } from '@tiptap/extension-text';

export const TextBaseExtension = TiptapBaseText.extend({});

export const BlockquoteBaseExtension = TiptapBaseBlockquote.extend({});

export const BoldBaseExtension = TiptapBaseBold.extend({});

export const BulletListBaseExtension = TiptapBaseBulletList.extend({});

export const CodeBaseExtension = TiptapBaseCode.extend({});

export const DropcursorBaseExtension = TiptapBaseDropcursor.extend({});

export const GapcursorBaseExtension = TiptapBaseGapcursor.extend({});

export const HardBreakBaseExtension = TiptapBaseHardBreak.extend({});

export const HeadingBaseExtension = TiptapBaseHeading.extend({});

export const HistoryBaseExtension = TiptapBaseHistory.extend({});

export const HorizontalRuleBaseExtension = TiptapBaseHorizontalRule.extend({});

export const ItalicBaseExtension = TiptapBaseItalic.extend({});

export const ListItemBaseExtension = TiptapBaseListItem.extend({});

export const OrderedListBaseExtension = TiptapBaseOrderedList.extend({});

export const ParagraphBaseExtension = TiptapBaseParagraph.extend({});

export const StrikeBaseExtension = TiptapBaseStrike.extend({});

export const ImageBaseExtension = TiptapImageExtension.extend({});

export const TableBaseExtension = TiptapBaseTable.extend({});

export const TableRowBaseExtension = TiptapBaseRow.extend({});

export const PlaceholderBaseExtension = TiptapBasePlaceholder.extend({});
